<Window x:Class="DBW.WPFClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DBW.WPFClient"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <DataGrid VirtualizingStackPanel.IsVirtualizing="True"
              VirtualizingStackPanel.VirtualizationMode="Recycling"
              ScrollViewer.CanContentScroll="True">
        <DataGrid.Columns>
            <DataGridTextColumn Header="Id" Binding="{Binding Id}" />
            <DataGridTextColumn Header="Nazwa" Binding="{Binding Nazwa}" />
            <DataGridTextColumn Header="Id nadrzędny" Binding="{Binding IdNadrzedny}" />
            <DataGridTextColumn Header="Id poziomu" Binding="{Binding IdPoziom}" />
            <DataGridTextColumn Header="Nazwa poziomu" Binding="{Binding NazwaPoziom}" />
            <DataGridTextColumn Header="Czy zmienne?" Binding="{Binding CzyZmienne}" />
        </DataGrid.Columns>
    </DataGrid>
</Window>
